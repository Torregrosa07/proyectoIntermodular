// Función que redirige a la página principal
function goHome() {
    // Redirige a la página principal (puedes cambiar la URL según necesites)
    window.location.href = "index.html"; // Redirige a "index.html" como ejemplo
  }
  
  // Asignamos el evento de clic al botón
  document.getElementById("homeButton").addEventListener("click", goHome);
  